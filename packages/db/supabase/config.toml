project_id = "portal"

[db]
port = 54322

[db.seed]
enabled = true
sql_paths = ["./seed.sql"]

[api]
port = 54321
schemas = ["public"]

[auth]
site_url = "http://localhost:3000"
enable_signup = true

[studio]
port = 54323

# Stripe calls this endpoint directly — no user JWT to verify; authenticity
# is proven by the webhook signature inside the function instead.
[functions.stripe-webhook]
verify_jwt = false
